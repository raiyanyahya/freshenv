[tox]
envlist = py37,mypy

[testenv]
deps =
  pytest
  mock
commands =
    pytest

[testenv:mypy]
skip_install = true
basepython = python3.7
deps =
  mypy
  types-requests
commands =
  mypy --ignore-missing-imports --pretty .
